# Adds python3 to the Nixpacks build environment so node-gyp can compile
# native modules during `npm install` (the `usb` package, pulled in by
# @solana/wallet-adapter-wallets for hardware wallet support, requires it).

[phases.setup]
nixPkgs = ["python3", "nodejs_20", "npm-9_x"]
